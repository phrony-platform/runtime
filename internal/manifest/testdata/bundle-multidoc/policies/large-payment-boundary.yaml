apiVersion: phrony.com/v1
kind: Policy

metadata:
  name: large-payment-boundary
  namespace: claims
  version: 1.0.0

spec:
  scope: tool:claims.approve-payment
  decision:
    type: allow
  allow:
    - USD
    - CAD
