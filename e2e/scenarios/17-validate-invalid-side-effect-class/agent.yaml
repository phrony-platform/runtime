apiVersion: phrony.com/v1
kind: Agent

metadata:
  name: payment-agent-bad-side-effect
  namespace: demo
  version: 1.0.0

spec:
  purpose: Invalid side_effect_class fixture.
  instructions:
    text: test
  model:
    provider: openai
    name: gpt-4o
  tools:
    - ref: payments.process-payment@1.0.0
      as: process_payment
      side_effect_class: destructive
