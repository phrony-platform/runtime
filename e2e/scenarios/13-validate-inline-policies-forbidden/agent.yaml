apiVersion: phrony.com/v1
kind: Agent

metadata:
  name: payment-agent-inline-policies
  namespace: demo
  version: 1.0.0

spec:
  purpose: Inline policies forbidden fixture.
  instructions:
    text: test
  model:
    provider: openai
    name: gpt-4o
  policies:
    - name: inline-deny
      scope: agent
      action: deny
  tools:
    - ref: payments.process-payment@1.0.0
      as: process_payment
