apiVersion: phrony.com/v1
kind: Agent

metadata:
  name: payment-agent-missing-policy
  namespace: demo
  version: 1.0.0

spec:
  purpose: Missing policy file fixture.
  instructions:
    text: test
  model:
    provider: openai
    name: gpt-4o
  default_policies:
    - demo.nonexistent-policy
  tools:
    - ref: payments.process-payment@1.0.0
      as: process_payment
