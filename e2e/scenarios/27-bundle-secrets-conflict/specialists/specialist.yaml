apiVersion: phrony.com/v1
kind: Agent

metadata:
  name: conflict-specialist
  namespace: demo
  version: 1.0.0

secrets:
  openai:
    fromEnv: OPENAI_API_KEY_DEV

spec:
  purpose: Specialist with conflicting openai fromEnv.
  instructions:
    text: Answer briefly.
  model:
    provider: stub
    name: scripted
    secret: openai
