apiVersion: phrony.com/v1
kind: Agent

metadata:
  name: payment-agent-dup-tools
  namespace: demo
  version: 1.0.0

spec:
  purpose: Duplicate tool ref fixture.
  instructions:
    text: test
  model:
    provider: openai
    name: gpt-4o
  default_policies:
    - demo.payment-require-approval
  tools:
    - ref: payments.process-payment@1.0.0
      as: process_payment
    - ref: payments.process-payment@1.0.0
      as: process_payment_2
