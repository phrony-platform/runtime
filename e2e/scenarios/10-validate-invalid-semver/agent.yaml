apiVersion: phrony.com/v1
kind: Agent

metadata:
  name: payment-agent-bad-semver
  namespace: demo
  version: not-a-version

spec:
  purpose: Invalid semver fixture.
  instructions:
    text: test
  model:
    provider: openai
    name: gpt-4o
  default_policies:
    - demo.payment-require-approval
  tools:
    - ref: payments.process-payment@1.0.0
      as: process_payment
