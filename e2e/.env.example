# Runtime gRPC endpoint (same as phrony CLI)
PHRONY_RUNTIME_ADDR=127.0.0.1:7777

# Advertised to the runtime on WorkRegister (must match allowlist when RUNTIME_TOOL_ALLOWLIST is set)
WORKER_ID=playground-worker-1
WORKER_WORKLOAD_IDENTITY=playground/local
WORKER_IMAGE_DIGEST=sha256:playground-dev
